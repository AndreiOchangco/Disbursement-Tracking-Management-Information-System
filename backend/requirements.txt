Django>=5.0,<7.0
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
PyJWT>=2.8.0

# Auth (if using token or JWT)
djangorestframework-simplejwt>=5.3.0

# Database (keep sqlite default, add this if switching to PostgreSQL)
psycopg2-binary>=2.9.9

# Environment variables
python-decouple>=3.8

# Image/file handling (if applicable)
Pillow>=10.0.0

# Dev tools (optional but recommended)
django-filter>=23.5